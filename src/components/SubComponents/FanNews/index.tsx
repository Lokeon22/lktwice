import { Container } from "./style";

interface FanNewsProps {
  notice: string;
  dates: string;
}

export const FanNews = ({ notice, dates }: FanNewsProps) => {
  return <Container></Container>;
};
